@model IEnumerable<WebStatisticSales.Models.SellerIndexView>

@{
    bool isValidForAction = (Request.IsAuthenticated && User.IsInRole("Admin"));
}

@if (Model.Count() > 0)
{
    <table class="table table-bordered">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Имя</th>
                @if (isValidForAction)
                {
                    <th scope="col">Действие</th>
                }
            </tr>
        </thead>
        <tbody>
            @foreach (var seller in Model)
            {
            <tr>
                <th scope="row">@seller.Id</th>
                <td>@seller.Name</td>
                @if (isValidForAction)
                {
                    <td>
                        <a href="#" onclick="return openEditForm(@seller.Id)">Edit</a> | <a href="#" onclick="return deleteSeller(@seller.Id)">Delete</a>
                    </td>
                }
            </tr>
            }
        </tbody>
    </table>
}
else
{
    <h2>База данных пуста.</h2>
}